#!/bin/bash

#This file is to give a "definition" of what a deep fake is.

echo "A deep fake essentially uses deep learning to take data and replicate it to create a compilation of data, all artificial yet resembling the original piece of data very similarly"
